# Perek 4 Internal Protected-Preview Packet Readiness - 2026-04-29

## Eligible for a later internal protected-preview packet

The following Perek 4 candidates are eligible for a later explicit internal protected-preview packet task:

- `g2srcdisc_p4_001` / `?????`
- `g2srcdisc_p4_002` / `????`
- `g2srcdisc_p4_003` / `???????`
- `g2srcdisc_p4_004` / `???????`

## Revision notes to preserve

- `g2srcdisc_p4_003`: keep spacing/session-balance note; do not place close to similar noun-recognition items.
- `g2srcdisc_p4_004`: keep as part-of-speech only, not vocabulary translation; preserve Minchah/offering alias review and spacing/session-balance warning.

## Blocked item

- `g2srcdisc_p4_005` / `????` remains `needs_source_follow_up` and must not be included in any later packet until explicitly resolved.

## What the next task may do

A later explicit task may create a small internal protected-preview packet from the four eligible items only, if all validators pass and the packet preserves the spacing/revision notes.

## What remains prohibited

- No Perek 4 runtime activation.
- No active scope expansion.
- No reviewed-bank promotion.
- No student-facing content.
- Internal protected-preview packet creation requires a later explicit task; this task creates readiness only.
- No advancement of `g2srcdisc_p4_005`.
